name: callr resuable workflwo
on: workflow_dispatch

jobs:
  job1:
    uses: ./.github/workflows/reusable.yaml
    with:
      month: 2
  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - run: echo "::notice::output -- ${{needs.job1.outputs.days}}"