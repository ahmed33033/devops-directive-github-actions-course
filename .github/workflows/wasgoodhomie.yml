name: environmental workflow

on: workflow_dispatch

env:
  work_env: rainbow

jobs:
  job1:
    env:
      job_env: toombon
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "wasgood $step_env"
          echo "wasgreat $job_env"
          echo "wasexcellent $work_env"
        env:
          step_env: odijf
        name: rainbow seige go go go
