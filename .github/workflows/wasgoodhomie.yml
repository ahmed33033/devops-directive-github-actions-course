name: matrix_workflow
on: 
  workflow_dispatch

concurrency:
  cancel-in-progress: true
  group: ${{github.workflow}}-${{github.ref}}

jobs:
  escape_the_matrix:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        a: [1,2,3]
        b: ['a', 'b', 'c']
    steps:
      - name: first step yo
        run: |
          echo "a: ${{matrix.a}}, b: ${{matrix.b}}"
      - name: check for specific combo
        if: ${{matrix.a == 1 && matrix.b == 'b'}}
        run: echo "sha SHINK"
      